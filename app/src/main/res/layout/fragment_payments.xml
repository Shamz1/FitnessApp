<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Payment History Title -->
    <TextView
        android:id="@+id/paymentHistoryTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Payment History"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Payment History RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/paymentHistoryRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/paymentHistoryTitle"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp" />

    <!-- Outstanding Payments Title -->
    <TextView
        android:id="@+id/outstandingPaymentsTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/paymentHistoryRecyclerView"
        android:layout_marginTop="16dp"
        android:text="Outstanding Payments"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Outstanding Payments RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/outstandingPaymentsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/outstandingPaymentsTitle"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp" />

    <!-- FloatingActionButton for adding payments -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddPayment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:src="@drawable/ic_add" />

</RelativeLayout>
